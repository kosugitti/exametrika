% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/24_M2.R
\name{add_M2}
\alias{add_M2}
\alias{add_M2.default}
\alias{add_M2.nominalLCA}
\alias{add_M2.ratedLCA}
\alias{add_M2.LRAordinal}
\alias{add_M2.ordinalBiclustering}
\alias{add_M2.nominalBiclustering}
\title{Attach the margin-based fit indices to a fitted model}
\usage{
add_M2(x, ...)

\method{add_M2}{default}(x, ...)

\method{add_M2}{nominalLCA}(x, verbose = TRUE, gc = TRUE, ...)

\method{add_M2}{ratedLCA}(x, verbose = TRUE, gc = TRUE, ...)

\method{add_M2}{LRAordinal}(x, verbose = TRUE, gc = TRUE, ...)

\method{add_M2}{ordinalBiclustering}(x, verbose = TRUE, gc = TRUE, ...)

\method{add_M2}{nominalBiclustering}(x, verbose = TRUE, gc = TRUE, ...)
}
\arguments{
\item{x}{A fitted model object of class "exametrika".}

\item{...}{Additional arguments passed to methods.}

\item{verbose}{Logical; if TRUE (default), reports the size of the margin
covariance matrix before computing it when that matrix is large.}

\item{gc}{Logical; if TRUE (default), releases the workspace before
returning. See \code{\link{M2}}.}
}
\value{
The fitted object with \code{TestFitIndicesM2} added.
}
\description{
Computes \code{\link{M2}} for the model and for the independence baseline, and
returns the fitted object with a \code{TestFitIndicesM2} component added. The
print method then shows the response-pattern indices and the margin-based ones
side by side.

This is a separate step rather than part of the fit because it is expensive:
the cost is the Cholesky factorisation of a dense matrix whose size grows with
the square of the item count (see \code{\link{M2}}).
}
\examples{
\donttest{
dat <- dataFormat(J20S600, response.type = "nominal")
fit <- LCA(dat, ncls = 3)
fit <- add_M2(fit)
fit
print(fit, fit_indices = "margin")
}

}
