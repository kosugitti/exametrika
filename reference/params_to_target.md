# convert parameters to optimization target

convert parameters to optimization target

## Usage

``` r
params_to_target(a_vec, b_list)
```

## Arguments

- a_vec:

  discrimination parameter vector

- b_list:

  threshold parameter list
